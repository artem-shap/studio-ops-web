# Base URL of studio-ops-api. Server-side only.
STUDIO_API_URL=http://localhost:8000

# Shared secret sent to studio-ops-api as the X-Studio-Key header.
# Server-side only. Never prefix this with NEXT_PUBLIC_.
STUDIO_API_KEY=
